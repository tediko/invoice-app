import Wrapper from '../Wrapper/Wrapper';
import Header from '../Header/Header';

const App = () => {
    return (
        <Wrapper>
            <Header />
        </Wrapper>
    );
};

export default App;
